% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{search_with_pagination}
\alias{search_with_pagination}
\title{Search with Pagination}
\usage{
search_with_pagination(
  tribunal,
  query,
  max_results = 100,
  size_per_request = 1000,
  api_key = NULL
)
}
\arguments{
\item{tribunal}{Character string with the tribunal alias.}

\item{query}{List containing the Elasticsearch query.}

\item{max_results}{Integer indicating the maximum total number of results to
retrieve (default: 100). Note: Each request can retrieve up to 10,000 results.}

\item{size_per_request}{Integer indicating the number of results per request
(default: 1000, maximum: 10000).}

\item{api_key}{Character string with the API key.}
}
\value{
A tibble with all retrieved results.
}
\description{
Search the DataJud API with automatic pagination to retrieve large result sets.
This function makes multiple requests to retrieve all results up to max_results.
}
\examples{
\dontrun{
# Set API key first
set_api_key(get_default_api_key())

# Search with pagination
query <- list(
  range = list(
    dataAjuizamento = list(
      gte = "2020-01-01",
      lte = "2020-01-31"
    )
  )
)
results <- search_with_pagination(
  tribunal = "api_publica_trf1",
  query = query,
  max_results = 5000
)
}
}
