% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_processes_paginated}
\alias{search_processes_paginated}
\title{Search Processes with Pagination}
\usage{
search_processes_paginated(
  tribunal,
  page_size = 100,
  max_pages = NULL,
  save_pages = FALSE,
  output_dir = "datajud_pages",
  output_format = "rds",
  ...,
  api_key = NULL
)
}
\arguments{
\item{tribunal}{Character string with the tribunal (e.g., "trf1", "tjsp").}

\item{page_size}{Integer indicating the number of results per page (default: 100, maximum: 10000).}

\item{max_pages}{Integer indicating the maximum number of pages to fetch (default: NULL for all available).}

\item{save_pages}{Logical indicating whether to save each page to disk (default: FALSE).}

\item{output_dir}{Character string with the directory to save pages (default: "datajud_pages").}

\item{output_format}{Character string with the output format: "rds", "csv", or "json" (default: "rds").}

\item{...}{Additional search parameters passed to search_processes (numeroProcesso, grau, classe_codigo, etc.).}

\item{api_key}{Character string with the API key.}
}
\value{
A list with:
\itemize{
\item data: Combined tibble with all results from all pages (if parse = TRUE)
\item pages: List of individual page results
\item total_hits: Total number of available results
\item pages_fetched: Number of pages actually fetched
\item files_saved: Vector of file paths if save_pages = TRUE
}
}
\description{
Comprehensive search function that supports pagination and saving results.
Allows fetching multiple pages of results and optionally saving each page to disk.
}
\examples{
\dontrun{
# Set API key first
set_api_key(get_default_api_key())

# Search with pagination
results <- search_processes_paginated(
  tribunal = "trf1",
  grau = "G2",
  page_size = 100,
  max_pages = 5
)

# Search and save each page
results <- search_processes_paginated(
  tribunal = "trf1",
  classe_codigo = "1199",
  page_size = 100,
  max_pages = 10,
  save_pages = TRUE,
  output_dir = "meus_dados",
  output_format = "csv"
)
}
}
