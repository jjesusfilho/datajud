% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_request.R
\name{search_datajud}
\alias{search_datajud}
\title{Search DataJud API}
\usage{
search_datajud(
  tribunal,
  query,
  size = 10,
  parse = TRUE,
  api_key = NULL,
  from = 0
)
}
\arguments{
\item{tribunal}{Character string with the tribunal alias. Use list_tribunals()
to see available options.}

\item{query}{List containing the Elasticsearch query. This should follow
Elasticsearch Query DSL format.}

\item{size}{Integer indicating the number of results to return (default: 10,
maximum: 10000).}

\item{parse}{Logical indicating whether to parse the results into a data frame
(default: TRUE).}

\item{api_key}{Character string with the API key. If NULL, uses the key set
with set_api_key() or the DATAJUD_API_KEY environment variable.}

\item{from}{Integer indicating the starting position for pagination (default: 0).}
}
\value{
If parse = TRUE, returns a tibble with the search results. If parse = FALSE,
returns the raw API response as a list.
}
\description{
General purpose function to search the DataJud API with custom queries.
}
\examples{
\dontrun{
# Set API key first
set_api_key(get_default_api_key())

# Search with a simple match query
query <- list(
  match = list(
    numeroProcesso = "00001234520204036100"
  )
)
results <- search_datajud("api_publica_trf1", query)
}
}
